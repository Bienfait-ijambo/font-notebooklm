// ui: {
//     // CSS style object applied to the <Handle /> normally
//     style?: React.CSSProperties,

//     // CSS style applied when this handle is highlighted as a valid target
//     highlightStyle?: React.CSSProperties,

//     // CSS style applied when a connection attempt to this handle was rejected
//     invalidStyle?: React.CSSProperties,

//     // CSS class names (optional alternative to style)
//     className?: string,
//     highlightClass?: string,
//     invalidClass?: string,

//     // boolean hints that help logic (optional)
//     highlightOnDrag?: boolean,    // true = should highlight while dragging
//     showBadge?: boolean,         // show a small badge (ex: number or error)
//     badgeContent?: string|number, 
//     maxConnections?: number,     // limit how many incoming/outgoing edges this handle accepts
//   }